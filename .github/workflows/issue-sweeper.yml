name: Issue Sweeper

on:
  workflow_dispatch:
  # --- DISABLED AUTOMATIC TRIGGERS (restore by uncommenting) ---
  # schedule:
  #   - cron: "0 6 * * 0"
  # --- END DISABLED ---

permissions:
  contents: read
  id-token: write
  issues: write
  pull-requests: read

jobs:
  sweep:
    uses: amatos/ai-workflows/.github/workflows/issue-sweeper.yml@main
    secrets: inherit
